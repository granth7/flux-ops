machine:
  kubelet:
    extraConfig:
      serializeImagePulls: false
    nodeIP:
      validSubnets:
        - 10.0.100.0/24
    extraMounts:
      - destination: /var/openebs
        type: bind
        source: /var/openebs
        options: ["bind", "rshared", "rw"]
